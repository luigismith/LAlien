# Python
tools/.venv/
__pycache__/
*.pyc
*.pyo

# Arduino build
firmware/build/
*.elf
*.bin
*.hex

# IDE
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Secrets — never commit API keys, device passwords, exported saves
sd_card_template/certs/*.pem
sd_card_template/certs/*.key
.env
.env.*
*.pem
*.key
*.pfx
deploy.env
.envrc
# Lalìen save exports (contain encrypted API keys + may include PIN hash)
*.lalien-save.json
# macOS / Windows lazy bits
.AppleDouble
ehthumbs.db

# Claude Code local settings — can include allowed-command patterns that
# embed inline credentials. NEVER commit.
.claude/settings.local.json
.claude/projects/
.claude/shell-snapshots/

# Generated sprites (large, regenerable)
# sd_card_template/sprites/ — uncomment if too large for git

# Logs
docs/*.log

# Screenshot helper (dev-only utility)
.capture_helper.py
